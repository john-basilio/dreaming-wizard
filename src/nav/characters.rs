// Placeholder stub for a future "Characters" nav page (mirroring
// `nav::canvas`'s `CanvasPage`/`CanvasMessage` shape). Not yet declared as
// a `mod` in `nav/mod.rs`, so this isn't compiled into the crate — it's
// intentionally unwired scaffolding, not a leftover.
pub fn sample() {}